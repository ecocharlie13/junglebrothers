// ⚠️ Adicione diretamente a classe como está no original
// 👇 Exemplo simplificado (você já tem o real, que é enorme)
function Gantt(selector, tasks, options = {}) {
  console.log("Gantt instanciado", selector, tasks, options);
  // Aqui vai toda a lógica original...
  // O que interessa é que esta função seja chamável com `new`
}

window.Gantt = Gantt;
